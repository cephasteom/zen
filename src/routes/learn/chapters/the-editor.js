export default `# The Editor
The code editor appears on the left and the console on the right. Icons below the header let you play or stop, open or save files and presets, and toggle the quantum circuit schematic. Clicking or typing in the editor hides the header.

Editor commands:
- \`shift+enter\` executes the code and starts playback
- \`esc\` stops playback.
- \`cmd+o\` opens presets and saved files
- \`cmd+s\` saves your code
`